Character sheet:
- Validate special powers like Magic, Druid, Shamanic or Religion. Do they produce cards?
- List a description of the powers.
- Add game rules about the use of powers.
- List possible actions due to powers or statistics.
- Show the corresponding statistic after specializations.

Allow closing and open the town and race selection on editing a character.
- Start the editor in that state when loading an existing character (with that choice already made)

Easier progression on a character:
- Allow increasing a current progression item with a button.
- Allow removing the last progression step with a button.

Extra info on progression:
- Show the effect of progression on the stats.
- Show the effect on XP. Skip the first 6 for XP.
- Allow entering the total available XP in a field.

Remove the rules about character generation/growth from the editor, it should already be clear from the implementation. Keep it around but closed by default.
- Move the rules section after the generator / roster.

Write rules about the use of powers and their relation with focus.
- Multiple use of a power increases the focus on it.
- This decreases the focus on other powers / statistics. Unless specifically stated in the power.
- With more than one point of focus it is given a penalty on actions outside the same statistic.
- Reducing this penalty again by defocusing the related statistic.
- The maximum focus is limited to the level in the power.
- Previous focus on the relevant statistic gives double growth.

Allow adding and editing contacts:
- Name, description and current town.
- In relation to organizations.
- With their own specializations.
- Complex contacts with a full character sheet.
- Shared with the whole group or only personally.



Map editor:
- DONE 2026-07-31. All three lines were one defect: a road is two half-edges and
  the table that mirrored the second half was the mirror of the COMPASS NAMES,
  not of the offsets those names label, so every road put its far half on an edge
  aimed at a third tile. Adjacent clicks were already broken too; a longer path
  only multiplied it. Pathfinding was never missing. See OPEN_ISSUES.md
  "World map editor" and test/lattice.test.js.
  - ~~Roads are broken.~~
  - ~~Allow click on adjacent tiles to link a road to them.~~
  - ~~This breaks when a non-adjacent tile is clicked.~~

Wall field names (left out of moros#3 on purpose):
- DONE 2026-08-11. `h_wall_n` -> `h_wall_nw` and `h_wall_se` -> `h_wall_e`, in
  `hex_voxel` and in `moros_map`, plus the serialised `sv_wall_*` and the stencil
  `sh_wall_*`. `h_wall_ne` was correct and did not move. 287 occurrences across
  39 files, in four structs in three packages. Verified: `make fast` 138 files,
  `make lib-test` 1600 tests over 11 packages on both backends with every
  per-package count identical, `make parts` byte-identical, `make gate` 47 PASS
  / 0 FAIL. The field ORDER and the byte layout are untouched, so no world file,
  part file or wire message changed -- which is what the byte-identical parts
  and the green gates say.
- ⚠ THE BLOCKER WAS NEVER REAL, which is why this sat for months. It said "PUBLIC
  fields of a published library (hex_world 0.2.0)", waiting on moros#8 to settle
  which tree owned the struct. That 0.2.0 is the registry's `hex_world`, a
  DIFFERENT lineage (plan 19 `L4`), with zero `h_wall` fields. Ours has never been
  published. There was no shared contract and nothing to wait for.
- ⚠ AND THE ESTIMATE WAS WRONG TOO: "~80 sites in ten files" was really 287 in
  39, because `moros_map` declares its OWN `Hex` carrying the same three misnamed
  fields, and `moros_editor` a `StencilHex` mirroring them. Two packages declare
  `pub struct Hex` with byte-identical field lists -- worth knowing on its own,
  and `tools/names.sh` is silent about it only because the two never meet in one
  dependency graph.
- The directions were re-derived from the lattice rather than copied from
  SCENE_MAP.md's table: slot -> slot_dir -> hex_grid::hex_neighbor and
  hex_edge_corners, cross-checked against the corner pairs the renderer spans.
  All agreed. hex_editor still reads the fields only through SLOT_NW/NE/E.
