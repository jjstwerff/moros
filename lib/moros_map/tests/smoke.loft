use moros_map;
fn test_hex() {
  h = Hex { h_height: 42, h_material: 5 };
  assert(h.h_height == 42, "height");
  assert(h.h_material == 5, "material");
}
fn test_map_empty() {
  m = map_empty();
  assert(m.m_name == "untitled", "name");
  assert(len(m.m_chunks) == 0, "no chunks");
}
